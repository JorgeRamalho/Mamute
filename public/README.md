# public/

Arquivos copiados para a raiz de `dist/` no build. Não passam pelo bundler.

| Arquivo | Papel |
| --- | --- |
| `favicon.svg` | Ícone do site (`index.html` → `./favicon.svg`). |
| `og.svg` | Imagem Open Graph / Twitter. |
| `robots.txt` | Crawlers; aponta o sitemap. |
| `sitemap.xml` | URLs canônicas das rotas da SPA. |
